# QuantLife AI 使用指南

## 🎯 快速开始步骤

### 第一步:安装后端依赖

打开命令提示符(CMD)或PowerShell,执行:

```bash
cd c:\Users\TX\Documents\Coding\projects\QuantLife-AI\backend
pip install -r requirements.txt
```

**注意**: 首次安装可能需要5-10分钟,请耐心等待。

### 第二步:测试数据获取

安装完成后,运行测试脚本验证数据获取功能:

```bash
cd c:\Users\TX\Documents\Coding\projects\QuantLife-AI\backend
python test_data_loader.py
```

这将:
- 从 yfinance 获取黄金和纳斯达克的历史数据(2010-2025)
- 计算统计指标(牛市/熊市阈值、均值)
- 保存数据到 `backend/data_cache/history_data.csv`

### 第三步:测试模拟引擎

```bash
python test_simulation.py
```

这将:
- 运行完整的资产模拟(2025.12 - 2029.06)
- 显示关键时间点的资产状况
- 验证业务逻辑(定投、稳健提取等)

### 第四步:启动后端服务

可以选择以下任一方式:

**方式1: 使用批处理脚本(推荐)**
```bash
cd c:\Users\TX\Documents\Coding\projects\QuantLife-AI
.\start-backend.bat
```

**方式2: 手动启动**
```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

启动成功后,你会看到:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
```

访问 http://localhost:8000/docs 可以查看API文档。

### 第五步:启动前端服务

**打开新的命令提示符窗口**,执行:

**方式1: 使用批处理脚本(推荐)**
```bash
cd c:\Users\TX\Documents\Coding\projects\QuantLife-AI
.\start-frontend.bat
```

**方式2: 手动启动**
```bash
cd frontend
npm run dev
```

启动成功后,访问显示的地址(通常是 http://localhost:5173)。

## 📊 使用系统

### 界面说明

1. **顶部控制面板**:
   - **市场情境**: 选择模拟场景(牛市/熊市/均线/历史回测)
   - **历史起始年份**: 仅在选择"历史回测"时显示
   - **统计卡片**: 显示初始资产、毕业时预计资产、初始月生活费

2. **图表区域**:
   - **左图 - 总资产趋势**: 堆叠柱状图,展示黄金、纳指、稳健、现金的变化
   - **右图 - 理论月均生活费**: 折线图,标注基准线(¥2000)和假期区域

### 操作步骤

1. **选择模拟场景**:
   - 🐂 **牛市**: 查看最乐观情况下的资产表现
   - 🐻 **熊市**: 查看最悲观情况下的资产表现
   - ⚖️ **均线**: 查看基于历史平均收益的稳健预测
   - 📅 **历史回测**: 选择历史某个时间点,模拟相同行情

2. **历史回测模式**:
   - 选择起始年份(如2015年)
   - 选择起始月份(如1月)
   - 系统将把2015年1月之后的真实行情映射到2025年12月之后

3. **查看结果**:
   - 鼠标悬停在图表上查看详细数据
   - 观察关键时间点(2026/2027/2028的9月)
   - 注意蓝色背景区域代表假期(不扣生活费)

## 🔍 数据解读

### 资产趋势图

- **黄金(黄色)**: 定投期间会逐渐减少,定投完成后相对稳定
- **纳指(紫色)**: 定投期间持续增长,后期是主要资产
- **稳健(蓝色)**: 每年9月会阶梯式下降,2028年9月后归零
- **现金(灰色)**: 初期用于生活费,很快耗尽

### 生活费趋势图

- **绿色区域**: 生活费高于基准线(¥2000),财务状况良好
- **红色区域**: 生活费低于基准线,需要注意资金管理
- **基准线**: 每月实际消费(¥2000)
- **蓝色背景**: 寒暑假,资产增值但不扣费

### 关键指标

- **初始总资产**: ¥142,400
- **毕业时预计总资产**: 根据市场情况变化
- **初始月生活费**: 约¥3,400(142400÷42个在校月)
- **最后月生活费**: 取决于最终资产

## ⚙️ 高级配置

### 修改初始资产

编辑 `backend/app/services/simulation.py` 中的 `AssetConfig` 类:

```python
class AssetConfig:
    def __init__(self):
        self.gold_val = 86000.0        # 修改初始黄金资产
        self.nasdaq_val = 16500.0      # 修改初始纳指资产
        self.safe_val = 37600.0        # 修改稳健投资
        self.cash_val = 2300.0         # 修改现金
```

### 修改定投策略

```python
self.nasdaq_max_investment = 60000.0  # 修改定投目标
self.daily_investment = 150.0         # 修改每日定投额
self.monthly_spend = 2000.0           # 修改月生活费
```

### 修改时间范围

```python
self.start_date = date(2025, 12, 1)   # 修改开始日期
self.end_date = date(2029, 6, 1)      # 修改结束日期
self.vacation_months = [1, 2, 8]      # 修改假期月份
```

修改后重启后端服务即可生效。

## 🐛 常见问题

### 1. 后端启动失败

**问题**: "ModuleNotFoundError: No module named 'xxx'"

**解决**: 确保已安装所有依赖
```bash
pip install -r requirements.txt
```

### 2. 数据获取失败

**问题**: "Failed to get data from yfinance"

**解决**: 
- 检查网络连接
- 尝试使用VPN
- 或手动下载历史数据CSV文件

### 3. 前端连接后端失败

**问题**: "Network Error" 或 CORS错误

**解决**:
- 确保后端服务已启动(http://localhost:8000)
- 检查防火墙设置
- 确认 `frontend/src/api/index.js` 中的API地址正确

### 4. 图表不显示

**问题**: 页面空白或图表区域空白

**解决**:
- 打开浏览器开发者工具(F12)查看错误信息
- 确保后端返回了数据
- 刷新页面(Ctrl+F5)

## 📝 开发建议

### 调试后端

1. 查看详细日志:
```bash
python -m uvicorn app.main:app --reload --log-level debug
```

2. 使用API文档测试:
访问 http://localhost:8000/docs

3. 直接运行测试脚本:
```bash
python test_simulation.py
```

### 调试前端

1. 打开浏览器开发者工具(F12)
2. 查看Console标签页的错误信息
3. 查看Network标签页的API请求

## 🎓 学习资源

- [FastAPI 官方文档](https://fastapi.tiangolo.com/)
- [Vue 3 官方文档](https://vuejs.org/)
- [Naive UI 组件库](https://www.naiveui.com/)
- [ECharts 图表库](https://echarts.apache.org/)
- [Pandas 数据处理](https://pandas.pydata.org/)

## 📧 获取帮助

遇到问题?
1. 查看本文档的"常见问题"部分
2. 查看 `README.md`
3. 检查代码注释
4. 提交 GitHub Issue

---

**祝你使用愉快!** 🎉
